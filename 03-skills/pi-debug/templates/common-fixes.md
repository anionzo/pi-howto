# Common Fixes

- Add null / undefined guards
- Await async work correctly
- Reset stale state between runs
- Validate input before processing
- Handle timeout and retry edges explicitly
- Add tests for the failing path
